<%  unless current_user.is_a? GuestUser %>
	<%= form_for @comments, url:  '#' do |f| %>
		<div class="form-group">
			<%= f.label :content %>
			<%= f.text_area :content, class:'form-control' %>
		</div>

		<%= f.submit 'Post Comment', class:'btn btn-cus' %>
	<% end %>
<% end %>